'use strict';

/*
** Note: It would be possible to loop through child folders
** and include folders dynamically if beneficial
*/

module.exports = {
  calc: require('./calc/calc'),
  save: require('./save/save'),
};